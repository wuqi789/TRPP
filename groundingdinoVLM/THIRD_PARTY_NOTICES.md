# Third-party notices

The public package contains ROS interfaces, deterministic mock providers and
provider-neutral adapter contracts. It does not include a detector checkpoint,
remote model client, inference runtime or private provider source.

Deployments that install an external perception adapter are responsible for
the adapter's upstream licenses, model terms, dependency notices and data
handling policy. Nothing in this package downloads or vendors those assets.
